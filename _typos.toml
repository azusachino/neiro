[files]
# Corpus vaults are third-party test data.
extend-exclude = ["packages/tests/vaults", "bun.lock"]

[default.extend-words]
# Ordinal suffixes, as in 1st, 2nd, 3rd.
nd = "nd"
# A deliberate typo: fuzzy-find tests check that a misspelled reference still gets suggestions.
laod = "laod"
